const number = 9;
const result = number % 2
let print = (result === 0) ? `${number} is an even number` : `${number} is an odd number`;

// if (result === 0) {
//     console.log(`${number} is an even number`);
// } else {
//     console.log(`${number} is an odd number`);
// }

console.log(print);