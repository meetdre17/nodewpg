const sideLength = 10;
const result = sideLength * 4;

console.log(`To calculate the perimeter of a square, we multiply the length of one side by four. For this square, the length is 10 so therefore, the perimeter is ${sideLength} * ${4} which is equal to`, result);