const sideLength = 5;
const result = sideLength * sideLength;

console.log(`To calculate the area of a square, we multiply the length of one side by itself. For this square, the length is 5 so, the area is ${sideLength} * ${sideLength} which is equal to`, result);