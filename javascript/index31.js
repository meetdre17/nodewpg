const firstSide = 10;
const secondSide = 20;
const thirdSide = 5;
const result = firstSide + secondSide + thirdSide;

console.log(`The perimeter of a triangle is the total distance around the outside, which can be found by adding together the length of each side. The perimeter for this triangle is ${firstSide} + ${secondSide} + ${thirdSide} which is equal to`, result);
